{
  "$id": "https://raw.githubusercontent.com/Vidushi-GitHub/gcn-avro-schema/validate/core-schema/gcn.notices.core.TimeRange.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "title": "gcn.notices.core.TimeRange",
  "description": "Generic time range schema in [s]",
  "properties": {
    "time_start": {
      "type": "number",
      "description": "Time window start, since trigger [s]"
    },
    "time_stop": {
      "type": "number",
      "description": "Time window stop, since trigger [s]"
    }
  },
  "required": ["time_start", "time_stop"]
}
