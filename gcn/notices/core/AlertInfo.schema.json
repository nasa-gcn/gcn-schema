{
  "$id": "https://gcn.nasa.gov/schema/gcn/notices/core/AlertInfo.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AlertInfo",
  "description": "Alert Information description properties of this notice",
  "type": "object",
  "properties": {
    "alert_datetime": {
      "type": "string",
      "description": "Date and time of notice creation [UTC, ISO 8601]"
    },
    "alert_tense": {
      "enum": ["current", "archival", "planned"],
      "description": "Indication of whether this alert refers to a recent observation (current), re-analysis of archival data (archival), or a planned observation in the future (planned)"
    },
    "alert_type": {
      "enum": ["initial", "update", "retraction"],
      "description": "Indication of alert sequence if multiple of same type sent"
    }
  },
  "required": ["alert_datetime", "alert_tense", "alert_type"]
}
