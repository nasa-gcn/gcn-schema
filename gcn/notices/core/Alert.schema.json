{
  "$id": "https://gcn.nasa.gov/schema/gcn/notices/core/Alert.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Alert",
  "description": "Alert Information description properties of this notice",
  "type": "object",
  "properties": {
    "datetime_of_notice": {
      "type": "string",
      "description": "Date and time of notice creation [UTC, ISO 8601]"
    },
    "alert_tense": {
      "enum": [
        "current",
        "archival",
        "planned",
        "injection",
        "commanded",
        "test"
      ],
      "description": "Indication of whether this alert refers to a recent observation (current), re-analysis of archival data (archival), or a planned observation in the future (planned), or an injection of sort."
    },
    "alert_type": {
      "enum": ["initial", "update", "retraction"],
      "description": "Indication of alert sequence if multiple of same type sent"
    }
  }
}
